.venv/
__pycache__/
*.pyc

# Рантайм и память флота: журнал вызовов, контекст проектов, выхлопы, загрузки.
# Инструмент публичный, а контекст рабочих проектов — нет, поэтому data/ живёт
# отдельным приватным репозиторием (AGENT.Dashboard.DATA), склонированным сюда:
#   git clone git@github.com:<логин>/AGENT.Dashboard.DATA.git data
data/

.env
.DS_Store
.idea/
