# Ключи флот берёт из окружения (у тебя они уже в ~/.zshrc).
GLM_API_KEY=...        # z.ai, работает только через coding-эндпоинт
DEEPSEEK_API_KEY=...   # api.deepseek.com, оплата по токенам

# Необязательное: локальный SearXNG для поиска получше, чем DuckDuckGo.
# docker run -d -p 8888:8080 searxng/searxng
# SEARXNG_URL=http://localhost:8888
